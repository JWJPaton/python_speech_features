A project to adapt the machine learning approach to automatically generating diphones for concatenative speech synthesis (found in a PhD thesis) into a technique for generating TTS voices from matched text/speech data.

Borrowing heavily from (= almost entirely identical to) the python_speech_features project.